include "stdlib.rbh";
include "stddefs.rbh";
include "../lib/query.rbh";

const YEAR_ACTUAL = 2020;
const RESPUESTA = true;
const MSG = "Stunx es real";

script main {
	// para probar el preprocesador
	// fmt() no puede recibir un segundo parámetro que no sea del tipo número
	// fmt("Incorrecto ->", -1)
	// si definimos la bandera 'CHECK_FMT_WIDTH' obtenemos el error:
	//     ./foker compiler/tests/foker_test.fkr -d CHECK_FMT_WIDTH
	var msg = fmt("Hola, probando el width"
#if CHECK_FMT_WIDTH // comentario al lado xd
		, -1
#endif
	);
	checkgender {
		boy {
			question "¿Eres chico?" {yes{}no{}}
		}
		girl {var girl_ = 1001;}
	}
}

